{% extends 'admin/base.html.twig' %}

{% block title %}Ajout d'un poste{% endblock %}

{% block body %}

    <div class="container">
        
        {{ form_start(form) }}

        <div class="row">
            <div class="col-md-10 offset-md-1">

                <a href="{{ path('admin_postes_index') }}" class="btn btn-primary">Retour à la liste des postes</a>
                <h1 class="my-5">Ajout d'un poste</h1>

                <div class="card">
                    <div class="card-body">
                        {{ form_row(form.title) }}

                        <button type="submit" class="btn btn-success float-right mt-2">
                            <i class="fas fa-check"></i> Ajouter le poste
                        </button>
                        <a href="{{ path('admin_postes_index') }}" class="btn btn-outline-danger float-right mr-1 mt-2">Annuler</a>      
                    </div>
                </div>
                
            </div>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}