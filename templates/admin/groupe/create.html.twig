{% extends 'admin/base.html.twig' %}

{% block title %}Ajout d'un groupe{% endblock %}

{% block body %}

    <div class="container">
        

        {{ form_start(form) }}

        <div class="row">
            <div class="col-md-10 offset-md-1">

                <a href="{{ path('admin_groupes_index') }}" class="btn btn-primary">Retour à la liste des groupes</a>
                <h1 class="my-5">Ajout d'un groupe</h1>

                <div class="card">
                    <div class="card-body">
                        {{ form_row(form.title) }}
                        {{ form_row(form.responsable) }}

                        <button type="submit" class="btn btn-success float-right mt-2">
                            <i class="fas fa-check"></i> Ajouter le groupe
                        </button>
                        <a href="{{ path('admin_groupes_index') }}" class="btn btn-outline-danger float-right mr-1 mt-2">Annuler</a>      
                    </div>
                </div>
                
            </div>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}