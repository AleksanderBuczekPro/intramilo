{% extends 'admin/base.html.twig' %}

{% block title %}Modification d'une catégorie{% endblock %}

{% block body %}

<div class="container">

    <div class="row">
        <div class="col-8 offset-md-2">
        
            <h1 class="my-h1">
                <a href="{{ path('admin_documentation_index') }}" class="btn btn-my-dark"><i class="uil uil-arrow-left"></i> Retour à la liste des sous-catégories</a> <br>
                {{ category.title }}
            </h1>

            {{ form_start(form) }}

                <div class="row">

                    {# Pole #}
                    <div class="col-2 text-right my-auto">
                        <span class="align-middle">{{ form_label(form.pole) }}</span>
                    </div>

                    <div class="col-10">
                        {{ form_widget(form.pole) }}
                    </div>

                    {# Titre #}
                    <div class="col-2 text-right my-auto">
                        <span class="align-middle">{{ form_label(form.title) }}</span>
                    </div>

                    <div class="col-10">
                        {{ form_widget(form.title) }}
                    </div>

                </div>

                <div class="mt-4 mb-4">
                    <button type="submit" class="btn btn-my-add-admin float-right"><i class="uil uil-plus"></i> Modifier le pôle</button>
                    <a href="{{ path('admin_documentation_index') }}" class="btn btn-my-danger float-right"><i class="uil uil-times"></i> Annuler</a>
                </div>

            {{ form_end(form) }}

        </div>
    </div>


</div>
{% endblock %}