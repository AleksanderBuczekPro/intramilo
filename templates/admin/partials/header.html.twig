 <nav class="navbar navbar-expand-lg sticky-top navbar-dark bg-primary">
            <a class="navbar-brand" href="{{ path('homepage') }}">Intramilo / BackOffice</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_users_index') }}">
                        <i class="fas fa-users"></i> Utilisateurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_groupes_index') }}">
                        <i class="fas fa-building"></i> Groupes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_antennes_index') }}">
                        <i class="fas fa-building"></i> Antennes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_postes_index') }}">
                        <i class="fas fa-building"></i> Postes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_poles_index') }}">
                        <i class="fas fa-stream"></i> Pôles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_category_index') }}">
                        <i class="fas fa-stream"></i> Catégories / Sous-Catégories</a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_category_index') }}">
                        <i class="fas fa-stream"></i> Documentation</a>
                    </li> #}
                    
                    
                </ul>   
                <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('homepage') }}" target="_blank" class="nav-link">
                        <i class="fas fa-eye"></i>
                        Voir le site
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item dropdown mt-2 mr-4">
                        <a class="nav-link notification" href="#" data-toggle="dropdown" id="accountDropdownLink">
                            <i class="fas fa-bell"></i>
                            <span class="mybadge">1</span>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">

                            <h6 class="dropdown-header">Compte</h6>
                            <a href="{{ path('account_index') }}" class="dropdown-item">Mon profil</a>
                            <a href="{{ path('account_picture_edit') }}" class="dropdown-item">Modifer ma photo de profil</a>
                            <a href="{{ path('account_password') }}" class="dropdown-item">Modifer mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">Documentation</h6>
                            <a href="{{ path('account_documents') }}" class="dropdown-item">Tableau de bord</a>

                            {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('admin_documents') }}" class="dropdown-item">Suivi de la documentation</a>
                            {% endif %}

                            {% if is_granted('ROLE_ADMIN') %}
                                <div class="dropdown-divider"></div>
                                <h6 class="dropdown-header">Administration</h6>
                                <a href="/admin" target="_blank" class="dropdown-item">BackOffice</a>
                            {% endif %}

                            <div class="dropdown-divider"></div>
                            <a href="{{ path('account_logout') }}" class="dropdown-item">Se déconnecter</a>
                        </div>
                    </li>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link" href="#" data-toggle="dropdown" id="accountDropdownLink">
                            {% include '_picture.html.twig' with {'user': app.user, 'class': 'medium'} %}
                            
                            {# {{ app.user.fullName }} #}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">

                            <h6 class="dropdown-header">Compte</h6>
                            <a href="{{ path('account_index') }}" class="dropdown-item">Mon profil</a>
                            <a href="{{ path('account_picture_edit') }}" class="dropdown-item">Modifer ma photo de profil</a>
                            <a href="{{ path('account_password') }}" class="dropdown-item">Modifer mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">Documentation</h6>
                            <a href="{{ path('account_documents') }}" class="dropdown-item">Tableau de bord</a>

                            {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('admin_documents') }}" class="dropdown-item">Suivi de la documentation</a>
                            {% endif %}

                            {% if is_granted('ROLE_ADMIN') %}
                                <div class="dropdown-divider"></div>
                                <h6 class="dropdown-header">Administration</h6>
                                <a href="/admin" target="_blank" class="dropdown-item">BackOffice</a>
                            {% endif %}

                            <div class="dropdown-divider"></div>
                            <a href="{{ path('account_logout') }}" class="dropdown-item">Se déconnecter</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('account_register') }}">Inscription</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('account_login') }}">Connexion</a>
                    </li>
                {% endif %}
                    
                </ul>              
            </div>
        </nav>

    {% for label, messages in app.flashes %}
        <div class="container">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                <p>{{ message | raw}}</p>    
                {% endfor %}
            </div>
        </div>
    {% endfor %}