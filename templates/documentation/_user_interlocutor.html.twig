<div class="card my-card interlocutor-card no-br p-1 m-0">
    <div class="card-body p-3">

        <table class="table my-table no-border p-0">
            <tr>
                <td class="p-0">
                    <div class="black main small">{{ user }}</div>
                    <div class="black my-secondary small op-70">{{ user.function }}</div>
                </td>
                {# <td class="interlocutor-contact p-0">
    
                    {% if user.phoneNumber or user.proNumber or user.email %}
                    <a data-toggle="collapse" href="#collapse{{user.id}}" role="button" aria-expanded="false" aria-controls="collapse{{user.id}}" class="violet small">
                        Contact
                    </a>

                    {% endif %}

                </td> #}
            </tr>

            {# <tr class="collapse multi-collapse" id="#phone{{user.id}}">
                <td>
                    {{ user.phoneNumber }}
                </td>
            </tr>

            <tr class="collapse multi-collapse" id="#mail{{user.id}}">
                <td>
                    {{ user.email }}
                </td>
            </tr> #}

                    
            {# <tr class="collapse" id="collapse{{user.id}}">
                <td class="px-0">

                    {% if user.email %}
                        <div class="my-secondary">
                            <a href="mailto:{{ user.email }}" class="mail-link"><i class="uil uil-envelope-alt"></i> {{ user.email }}</a>
                        </div> 
                    {% endif %}

                    {% if user.phoneNumber %}
                        <div class="my-secondary"><i class="uil uil-phone-alt"></i> {{ user.phoneNumber }}</div>
                    {% endif %}
                    {% if user.proNumber %}
                        <div class="my-secondary"><i class="uil uil-phone-alt"></i> {{ user.proNumber }}</div>
                    {% endif %}


                </td>
            </tr> #}

            <tr>
                <td class="px-0 pt-2">

                    {% if user.email %}
                        <div class="my-secondary">
                            <a href="mailto:{{ user.email }}" class="mail-link"><i class="uil uil-envelope-alt"></i> {{ user.email }}</a>
                        </div> 
                    {% endif %}

                    {% if user.phoneNumber %}
                        <div class="my-secondary"><i class="uil uil-phone-alt"></i> {{ user.phoneNumber }}</div>
                    {% endif %}
                    {% if user.proNumber %}
                        <div class="my-secondary"><i class="uil uil-phone-alt"></i> {{ user.proNumber }}</div>
                    {% endif %}
                </td>
            </tr>
        
        </table>


        

        {# <div class="row">
            <div class="col">
                <div class="collapse multi-collapse" id="multiCollapseExample1">
                <div class="card card-body">
                    Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica, craft beer labore wes anderson cred nesciunt sapiente ea proident.
                </div>
                </div>
            </div>
            <div class="col">
                <div class="collapse multi-collapse" id="multiCollapseExample2">
                <div class="card card-body">
                    Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica, craft beer labore wes anderson cred nesciunt sapiente ea proident.
                </div>
                </div>
            </div>
        </div> #}


        {# <div class="marine my-secondary small mt-2">{{ user.phoneNumber }}</div>
        <div class="marine my-secondary small">{{ user.email }}</div> #}

    </div>
</div>