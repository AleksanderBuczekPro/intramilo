<div class="card my-card user">
    <div class="card-body p-0">   

        <table class="my-table">
            <tbody>
                <tr>
                    <td class="p-2">
                        {% include '_picture.html.twig' with {'user': user, 'class': 'medium'} %}
                    </td>
                    <td class="text-left pl-2">
                        <div class="my-secondary main">{{ user }}</div>
                        <div class="my-secondary">{{ user.function }}</div>
                    </td>
                    
                </tr>
            </tbody>
        </table>

    </div>
</div>